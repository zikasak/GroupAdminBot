pipeline {
    agent {
        docker { image 'maven:3.8.1-adoptopenjdk-15'
        args '-v /root/.m2:/root/.m2'}
    }
    stages {
        stage("Build") {
            steps {
                withMaven {
                    sh 'mvn -DskipTests -B clean package'
                }
            }
        }

    }
}